#include <stdio.h>

int main(){
    int r = 1;
    int h =2;
    printf("The area of the circle of radius %d is %f\n",r,3.14*r*r);
    printf("The volume of the clynder of radius %d and h %d is %f",r,h,3.14*r*r*h);
    return 0;
}